{% extends 'base.html.twig' %}

{% block title %}Rabeh{% endblock %}

{% block body %}
    <a href="{{path('ajouteEtudiant')}}"><h2>Ajout Etudiant </h2></a>
    <hr><br>
    <form action="{{ path('rechercheEtudiant') }}">
        @Email :
        <input type="text" name="email" required="required">
        <button type="submit" class="btn btn-primary">Recherche</button>
    </form>
    <table class="table table-hover">
        <thead>
        <tr>
            <th scope="col">id</th>
            <th scope="col">nom</th>
            <th scope="col">email</th>
            <th scope="col">class</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        {% for etudiant in liste %}
            <tr>
                <th>{{etudiant.id}}</th>
                <th>{{etudiant.nom}}</th>
                <th>{{etudiant.email}}</th>
                <th>{{etudiant.class}}</th>
                <th>
                 <a href="{{path('detailEtudiant',{'id':etudiant.id})}}">
                        <button type="button" class="btn btn-primary">plus de détails</button>
                    </a>
                    <a href="{{path('modificationEtudiant',{'id':etudiant.id})}}">
                        <button type="button" class="btn btn-warning">Modification</button>
                    </a>
                    <a href="{{path('supprimerEtudiant',{'id' :etudiant.id})}}">
                        <button type="button" class="btn btn-danger">Supprimer</button>
                    </a>
                </th>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}